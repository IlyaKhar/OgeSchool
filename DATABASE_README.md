# База данных заданий для платформы ЕГЭ

## Обзор

Вместо использования нейронной сети для генерации заданий, платформа теперь использует структурированную базу данных SQLite для хранения и управления заданиями. Это обеспечивает:

- ✅ **Контролируемое качество** - все задания проверены экспертами
- ✅ **Быстрая работа** - мгновенная генерация вариантов
- ✅ **Масштабируемость** - легко добавлять новые задания
- ✅ **Аналитика** - детальная статистика по каждому заданию
- ✅ **Персонализация** - адаптация под уровень ученика

## Структура базы данных

### Основные таблицы:

1. **subjects** - предметы (математика, русский язык, физика и т.д.)
2. **topics** - темы внутри предметов (алгебра, геометрия, орфография и т.д.)
3. **tasks** - задания с вопросами, ответами и объяснениями
4. **answer_options** - варианты ответов для заданий с множественным выбором
5. **test_variants** - готовые варианты тестов
6. **variant_tasks** - связь вариантов с заданиями
7. **users** - пользователи платформы
8. **test_results** - результаты прохождения тестов
9. **user_answers** - ответы пользователей на конкретные задания
10. **user_progress** - прогресс пользователей по темам

## Установка и настройка

### 1. Автоматическая установка
```bash
node setup-database.js
```

### 2. Ручная установка
```bash
# Установка зависимостей
npm install better-sqlite3 sqlite3

# Инициализация базы данных
node database/init.js
```

## API Endpoints

### Предметы и темы
- `GET /api/subjects` - получить все предметы
- `GET /api/subjects/:id/topics` - получить темы по предмету
- `GET /api/subjects/:id/tasks` - получить задания по предмету

### Задания
- `GET /api/tasks/:id` - получить конкретное задание с вариантами ответов
- `GET /api/topics/:id/tasks` - получить задания по теме

### Варианты тестов
- `GET /api/variants` - получить все варианты тестов
- `GET /api/variants/:id` - получить конкретный вариант с заданиями
- `POST /api/variants/generate` - сгенерировать случайный вариант

### Результаты
- `POST /api/test-results` - сохранить результат тестирования
- `GET /api/users/:id/results` - получить результаты пользователя
- `GET /api/users/:id/progress` - получить прогресс пользователя

## Типы заданий

1. **multiple_choice** - с множественным выбором
2. **short_answer** - краткий ответ
3. **detailed_answer** - развернутый ответ
4. **matching** - на соответствие
5. **ordering** - на упорядочивание

## Уровни сложности

- **1** - Базовый уровень
- **2** - Повышенный уровень
- **3** - Высокий уровень
- **4** - Очень высокий уровень
- **5** - Экспертный уровень

## Примеры использования

### Получить все предметы
```javascript
fetch('/api/subjects')
  .then(response => response.json())
  .then(data => console.log(data.subjects));
```

### Сгенерировать случайный вариант по математике
```javascript
fetch('/api/variants/generate', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    subjectId: 1, // ID математики
    taskCount: 20,
    difficultyDistribution: {
      1: 4,  // 4 задания базового уровня
      2: 6,  // 6 заданий повышенного уровня
      3: 6,  // 6 заданий высокого уровня
      4: 3,  // 3 задания очень высокого уровня
      5: 1   // 1 задание экспертного уровня
    }
  })
})
.then(response => response.json())
.then(data => console.log(data.tasks));
```

### Сохранить результат тестирования
```javascript
fetch('/api/test-results', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    userId: 1,
    variantId: 1,
    answers: [
      { taskId: 1, userAnswer: '4' },
      { taskId: 2, userAnswer: '2;3' },
      // ... другие ответы
    ],
    timeSpent: 1800 // в секундах
  })
})
.then(response => response.json())
.then(data => console.log(data));
```

## Добавление новых заданий

### Через SQL
```sql
INSERT INTO tasks (subject_id, topic_id, task_type, difficulty_level, points, question_text, correct_answer, explanation) 
VALUES (1, 1, 'multiple_choice', 2, 1, 'Решите уравнение: 3x + 7 = 22', '5', 'Переносим 7 в правую часть и делим на 3');

-- Добавляем варианты ответов
INSERT INTO answer_options (task_id, option_text, is_correct, order_index) 
VALUES (LAST_INSERT_ROWID(), '4', 0, 1),
       (LAST_INSERT_ROWID(), '5', 1, 2),
       (LAST_INSERT_ROWID(), '6', 0, 3),
       (LAST_INSERT_ROWID(), '7', 0, 4);
```

### Через API (планируется)
```javascript
// В будущем можно будет добавлять задания через веб-интерфейс
```

## Преимущества перед нейронной сетью

| Аспект | База данных | Нейронная сеть |
|--------|-------------|----------------|
| **Качество** | ✅ Проверено экспертами | ❓ Непредсказуемо |
| **Скорость** | ✅ Мгновенно | ⏳ Требует времени |
| **Стоимость** | ✅ Бесплатно | 💰 Дорого |
| **Контроль** | ✅ Полный контроль | ❌ Ограниченный |
| **Аналитика** | ✅ Детальная статистика | ❌ Сложно отследить |
| **Персонализация** | ✅ Гибкая настройка | ❓ Ограниченная |

## Мониторинг и аналитика

База данных автоматически собирает статистику:
- Количество попыток по каждому заданию
- Процент правильных ответов
- Время выполнения заданий
- Прогресс пользователей по темам
- Проблемные области для каждого ученика

## Резервное копирование

```bash
# Создать резервную копию
cp database/tasks.db database/backup_$(date +%Y%m%d_%H%M%S).db

# Восстановить из резервной копии
cp database/backup_20241201_120000.db database/tasks.db
```

## Расширение функциональности

### Планируемые улучшения:
- [ ] Веб-интерфейс для добавления заданий
- [ ] Импорт заданий из Excel/CSV
- [ ] Система тегов для заданий
- [ ] Адаптивное тестирование
- [ ] Экспорт результатов в PDF
- [ ] Интеграция с внешними источниками заданий

## Поддержка

При возникновении проблем:
1. Проверьте логи сервера
2. Убедитесь, что база данных инициализирована
3. Проверьте права доступа к файлам
4. Обратитесь к разработчикам

---

**База данных заданий** - это надежная и масштабируемая альтернатива нейронным сетям для образовательных платформ.

