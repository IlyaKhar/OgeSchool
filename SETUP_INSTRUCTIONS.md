# 🚀 Инструкция по запуску платформы ЕГЭ с базой данных

## 📋 Что было реализовано

### ✅ База данных заданий
- **SQLite база данных** с полной структурой для заданий, вариантов и результатов
- **10 предметов** с темами и готовыми заданиями
- **API endpoints** для всех операций с базой данных
- **Автоматическая генерация** случайных вариантов тестов

### ✅ Новые страницы
- **`tasks-database.html`** - работа с заданиями из базы данных
- **`test-page.html`** - прохождение тестов с таймером
- **`js/database-api.js`** - клиент для работы с API

### ✅ Интеграция
- **Обновленный дашборд** с данными из БД
- **Навигация** между всеми страницами
- **Автоматическая настройка** базы данных

## 🛠️ Пошаговая установка

### 1. Установка зависимостей
```bash
# Установка всех необходимых пакетов
npm install
```

### 2. Настройка базы данных
```bash
# Автоматическая настройка (рекомендуется)
node setup-database.js

# Или вручную
npm run setup-db
```

### 3. Запуск сервера
```bash
# Запуск в продакшн режиме
npm start

# Или в режиме разработки
npm run dev
```

### 4. Открытие в браузере
```
http://localhost:3000
```

## 📱 Как использовать

### 🎯 Работа с заданиями
1. Перейдите на **"База заданий"** в меню
2. Выберите предмет (математика, русский язык, физика и др.)
3. Выберите тему или сгенерируйте случайный вариант
4. Решайте задания с проверкой ответов

### 📝 Прохождение тестов
1. На странице "База заданий" нажмите **"Сгенерировать вариант"**
2. Откроется новая вкладка с тестом
3. Решайте задания по порядку
4. В конце увидите результаты с оценкой

### 📊 Личный кабинет
1. Войдите в систему (кнопка "Войти")
2. В дашборде увидите статистику из базы данных
3. Просматривайте прогресс по предметам

## 🔧 API Endpoints

### Предметы и темы
- `GET /api/subjects` - все предметы
- `GET /api/subjects/:id/topics` - темы по предмету
- `GET /api/subjects/:id/tasks` - задания по предмету

### Задания
- `GET /api/tasks/:id` - конкретное задание
- `GET /api/topics/:id/tasks` - задания по теме

### Тесты
- `GET /api/variants` - все варианты тестов
- `GET /api/variants/:id` - конкретный вариант
- `POST /api/variants/generate` - генерация случайного варианта

### Результаты
- `POST /api/test-results` - сохранение результата
- `GET /api/users/:id/results` - результаты пользователя
- `GET /api/users/:id/progress` - прогресс пользователя

## 📊 Структура базы данных

```
subjects (предметы)
├── topics (темы)
│   └── tasks (задания)
│       └── answer_options (варианты ответов)
├── test_variants (варианты тестов)
│   └── variant_tasks (связь вариантов с заданиями)
├── users (пользователи)
├── test_results (результаты тестов)
├── user_answers (ответы пользователей)
└── user_progress (прогресс по темам)
```

## 🎨 Типы заданий

1. **multiple_choice** - с множественным выбором
2. **short_answer** - краткий ответ
3. **detailed_answer** - развернутый ответ
4. **matching** - на соответствие
5. **ordering** - на упорядочивание

## 📈 Уровни сложности

- **1** - Базовый уровень
- **2** - Повышенный уровень  
- **3** - Высокий уровень
- **4** - Очень высокий уровень
- **5** - Экспертный уровень

## 🔍 Примеры использования

### Генерация случайного варианта по математике
```javascript
const variant = await databaseAPI.generateRandomVariant(1, {
  taskCount: 20,
  difficultyDistribution: {
    1: 4,  // 4 задания базового уровня
    2: 6,  // 6 заданий повышенного уровня
    3: 6,  // 6 заданий высокого уровня
    4: 3,  // 3 задания очень высокого уровня
    5: 1   // 1 задание экспертного уровня
  }
});
```

### Сохранение результата теста
```javascript
const result = await databaseAPI.saveTestResult({
  userId: 1,
  variantId: 1,
  answers: [
    { taskId: 1, userAnswer: '4' },
    { taskId: 2, userAnswer: '2;3' }
  ],
  timeSpent: 1800 // в секундах
});
```

## 🚨 Устранение неполадок

### Ошибка "База данных не найдена"
```bash
# Пересоздайте базу данных
node database/init.js
```

### Ошибка "Модуль не найден"
```bash
# Переустановите зависимости
npm install
```

### Порт 3000 занят
```bash
# Измените порт в .env файле
PORT=3001
```

## 📁 Файловая структура

```
EGE/
├── database/
│   ├── schema.sql          # Схема базы данных
│   ├── sample_data.sql     # Примеры данных
│   ├── database.js         # Модуль работы с БД
│   └── init.js            # Инициализация БД
├── js/
│   └── database-api.js    # API клиент
├── tasks-database.html    # Страница заданий
├── test-page.html         # Страница тестов
├── setup-database.js      # Скрипт настройки
└── server.js              # Обновленный сервер
```

## 🎉 Готово!

Теперь ваша платформа ЕГЭ работает с **реальной базой данных** вместо нейронной сети:

- ✅ **Мгновенная работа** - нет задержек на генерацию
- ✅ **Контролируемое качество** - все задания проверены
- ✅ **Детальная аналитика** - статистика по каждому заданию
- ✅ **Персонализация** - адаптация под уровень ученика
- ✅ **Масштабируемость** - легко добавлять новые задания

**Наслаждайтесь новой функциональностью!** 🚀
