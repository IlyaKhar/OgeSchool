# Гайд по деплою на Vercel

## Шаг 1: Подготовка проекта

### 1.1 Убедитесь, что проект в GitHub
```bash
# Проверьте, что все изменения закоммичены
git status

# Если есть изменения, закоммитьте их
git add .
git commit -m "Prepare for Vercel deployment"
git push origin main
```

### 1.2 Создайте файл `vercel.json` (опционально)
Vercel автоматически определит Express проект, но можно создать конфигурацию:

```json
{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server.js"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
```

## Шаг 2: Импорт проекта в Vercel

1. **Зайдите на [vercel.com](https://vercel.com)** и войдите через GitHub
2. **Нажмите "New Project"**
3. **Выберите репозиторий** `IlyaKhar/OgeSchool`
4. **Настройки проекта:**
   - **Framework Preset**: Express (должен определиться автоматически)
   - **Root Directory**: `./` (корень проекта)
   - **Project Name**: `oge-school` (или любое другое имя)

## Шаг 3: Настройка переменных окружения

**ВАЖНО!** Перед деплоем обязательно добавьте все переменные окружения:

1. В форме создания проекта нажмите **"Environment Variables"**
2. Добавьте следующие переменные:

### Обязательные переменные:

```
PORT=3000
NODE_ENV=production
```

### База данных MongoDB:

```
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/ege-platform?retryWrites=true&w=majority
```

**Как получить MongoDB URI:**
1. Зайдите на [MongoDB Atlas](https://www.mongodb.com/cloud/atlas) (бесплатный аккаунт)
2. Создайте кластер (бесплатный M0)
3. Создайте пользователя базы данных
4. Добавьте IP адрес `0.0.0.0/0` в Network Access (для Vercel)
5. Скопируйте Connection String и замените `<password>` на пароль

### JWT и безопасность:

```
JWT_SECRET=ваш-очень-длинный-случайный-секретный-ключ-минимум-32-символа
JWT_REFRESH_SECRET=другой-длинный-секретный-ключ-для-refresh-токенов
```

**Сгенерируйте секреты:**
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### AI API ключи (опционально, но рекомендуется):

```
OPENAI_API_KEY=sk-ваш-ключ-openai
# или
DEEPSEEK_API_KEY=ваш-ключ-deepseek
```

### Email (SMTP) для обратной связи:

```
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ilyuha1712@gmail.com
SMTP_PASS=ваш-пароль-приложения-gmail
```

**Как получить пароль приложения Gmail:**
1. Включите 2FA в Google аккаунте
2. Перейдите в [Настройки аккаунта](https://myaccount.google.com/)
3. Безопасность → Двухэтапная аутентификация → Пароли приложений
4. Создайте пароль для "Почта" и "Другое устройство"
5. Используйте этот пароль в `SMTP_PASS`

### Админ панель:

```
ADMIN_EMAIL=alina@gmail.com
```

### Stripe (опционально, для платежей):

```
STRIPE_SECRET_KEY=sk_live_ваш-секретный-ключ
STRIPE_PUBLISHABLE_KEY=pk_live_ваш-публичный-ключ
```

## Шаг 4: Настройка Build Settings

В разделе **"Build and Output Settings"**:

- **Build Command**: оставьте пустым (Vercel определит автоматически)
- **Output Directory**: оставьте пустым
- **Install Command**: `npm install`

## Шаг 5: Деплой

1. Нажмите кнопку **"Deploy"**
2. Дождитесь завершения сборки (обычно 1-3 минуты)
3. После успешного деплоя вы получите URL вида: `https://oge-school.vercel.app`

## Шаг 6: Проверка после деплоя

### 6.1 Проверьте работу API:
```bash
curl https://oge-school.vercel.app/api/health
```

### 6.2 Проверьте главную страницу:
Откройте в браузере: `https://oge-school.vercel.app`

### 6.3 Проверьте логи:
1. В панели Vercel перейдите в **"Deployments"**
2. Выберите последний деплой
3. Откройте вкладку **"Logs"**
4. Проверьте, что нет ошибок подключения к MongoDB

## Шаг 7: Настройка домена (опционально)

1. В настройках проекта перейдите в **"Domains"**
2. Добавьте свой домен
3. Следуйте инструкциям по настройке DNS

## Важные замечания для Vercel

### ⚠️ Ограничения Vercel:

1. **Serverless Functions**: Vercel использует serverless функции, которые имеют ограничения:
   - Максимальное время выполнения: 10 секунд (Hobby план) или 60 секунд (Pro)
   - Максимальный размер ответа: 4.5 MB

2. **SQLite база данных**: 
   - SQLite файлы (`*.db`) не сохраняются между вызовами в serverless окружении
   - Рекомендуется использовать только MongoDB для продакшена
   - Если нужен SQLite, рассмотрите использование внешнего хранилища (S3, R2)

3. **Статические файлы**:
   - Все HTML, CSS, JS файлы будут раздаваться как статика
   - Убедитесь, что пути к файлам корректны

### 🔧 Рекомендации:

1. **Используйте MongoDB Atlas** для базы данных (бесплатный план M0)
2. **Настройте CORS** правильно в `src/app.js`:
   ```javascript
   app.use(cors({
     origin: ['https://oge-school.vercel.app', 'https://your-domain.com'],
     credentials: true
   }));
   ```

3. **Проверьте переменные окружения** после каждого деплоя

4. **Используйте Vercel CLI** для локальной разработки:
   ```bash
   npm i -g vercel
   vercel dev
   ```

## Обновление проекта

После каждого `git push` в ветку `main`:
- Vercel автоматически создаст новый деплой
- Проверьте логи на наличие ошибок
- Если что-то сломалось, можно откатиться к предыдущему деплою

## Troubleshooting

### Проблема: "Cannot find module"
**Решение**: Убедитесь, что все зависимости в `package.json` и выполните `npm install` локально перед коммитом

### Проблема: MongoDB не подключается
**Решение**: 
- Проверьте `MONGODB_URI` в переменных окружения
- Убедитесь, что IP `0.0.0.0/0` добавлен в MongoDB Atlas Network Access
- Проверьте логи в Vercel

### Проблема: 404 на API роутах
**Решение**: 
- Убедитесь, что `vercel.json` настроен правильно
- Проверьте, что роуты начинаются с `/api/`

### Проблема: CORS ошибки
**Решение**: 
- Добавьте домен Vercel в `cors` настройки
- Проверьте `credentials: true` в CORS конфигурации

## Полезные команды

```bash
# Установка Vercel CLI
npm i -g vercel

# Локальная разработка с Vercel
vercel dev

# Просмотр логов
vercel logs

# Просмотр переменных окружения
vercel env ls
```

## Следующие шаги

После успешного деплоя:
1. ✅ Протестируйте регистрацию и вход
2. ✅ Проверьте работу AI-чата
3. ✅ Протестируйте админ-панель
4. ✅ Проверьте отправку email через форму обратной связи
5. ✅ Настройте мониторинг и алерты

Удачи с деплоем! 🚀

