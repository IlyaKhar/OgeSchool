# 🚀 Инструкция по установке OpenAI API

## Шаг 1: Получение API ключа OpenAI

1. Перейдите на [OpenAI Platform](https://platform.openai.com/api-keys)
2. Войдите в свой аккаунт или создайте новый
3. Нажмите "Create new secret key"
4. Скопируйте полученный ключ (он начинается с `sk-`)

## Шаг 2: Настройка проекта

1. Создайте файл `.env` в корневой папке проекта:
```bash
# Windows
copy env.example .env

# Linux/Mac
cp env.example .env
```

2. Откройте файл `.env` и замените `your_openai_api_key_here` на ваш реальный ключ:
```
OPENAI_API_KEY=sk-your_actual_api_key_here
PORT=3000
```

## Шаг 3: Установка зависимостей

```bash
npm install
```

## Шаг 4: Запуск сервера

```bash
# Режим разработки (с автоперезагрузкой)
npm run dev

# Или продакшн режим
npm start
```

## Шаг 5: Проверка работы

1. Откройте браузер и перейдите на `http://localhost:3000`
2. Зарегистрируйтесь или войдите в систему
3. Перейдите в личный кабинет
4. Попробуйте AI-функции:
   - Задайте вопрос в AI-помощнике
   - Посмотрите персональные рекомендации
   - Попробуйте объяснение темы

## 🔧 Устранение проблем

### Ошибка "OpenAI API недоступен"
- Проверьте правильность API ключа
- Убедитесь, что у вас есть кредиты на счете OpenAI
- Проверьте подключение к интернету

### Ошибка "Сервер не запускается"
- Убедитесь, что порт 3000 свободен
- Проверьте, что все зависимости установлены
- Посмотрите логи в консоли

### AI функции не работают
- Проверьте консоль браузера на ошибки
- Убедитесь, что сервер запущен
- Проверьте файл `.env`

## 💰 Стоимость использования

OpenAI API платный:
- GPT-4: ~$0.03 за 1K токенов (вход) + $0.06 за 1K токенов (выход)
- Примерно $0.01-0.05 за один запрос

**Рекомендация**: Начните с небольшой суммы ($5-10) для тестирования.

## 🔒 Безопасность

- Никогда не публикуйте ваш API ключ
- Не добавляйте `.env` в git репозиторий
- Используйте разные ключи для разработки и продакшена

## 📞 Поддержка

При проблемах:
1. Проверьте [документацию OpenAI](https://platform.openai.com/docs)
2. Посмотрите логи сервера
3. Создайте Issue в репозитории проекта
